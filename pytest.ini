[pytest]
addopts = -p no:trio
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
	integration: marks tests as integration tests for slower end-to-end checks
	agent_server: marks tests that require a running LangGraph Agent Server (langgraph dev)